# ---- Phase 1: mathematical core (required now) ----
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
pytest>=7.0

# ---- Phase 2: data / ML / regimes ----
pandas>=2.0
yfinance>=0.2.40
fredapi>=0.5
xgboost>=2.0          # needs an OpenMP runtime (`brew install libomp` on macOS)
hmmlearn>=0.3
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3     # GradientBoosting fallback if xgboost can't load

# ---- Phase 2: API service (FastAPI + Redis + WebSocket) ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
redis>=5.0            # API degrades to an in-process cache if redis is unreachable
httpx>=0.27           # used by fastapi TestClient in tests
