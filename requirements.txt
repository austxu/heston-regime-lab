# Runtime and research dependencies.  Lower bounds reflect Python 3.12 support; upper
# bounds prevent an unreviewed future major release from silently changing production.
# Developer/test tools live in requirements-dev.txt.

# ---- Mathematical core ----
numpy>=1.26,<3
scipy>=1.11,<2
pyyaml>=6.0,<7

# ---- Data / ML / regimes ----
pandas>=2.1,<4
yfinance>=0.2.40,<2
fredapi>=0.5,<1
xgboost>=2.0,<4       # needs OpenMP (`brew install libomp` on macOS)
hmmlearn>=0.3,<1
scikit-learn>=1.4,<2  # GradientBoosting fallback if xgboost cannot load

# ---- API service (FastAPI + Redis + WebSocket) ----
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.6,<3
redis>=5.0,<9          # API degrades to an in-process cache if Redis is unreachable
sentry-sdk>=2.0,<3     # no-op unless SENTRY_DSN is set
