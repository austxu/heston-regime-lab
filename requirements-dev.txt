# Reuse the production dependency graph, then add tools that are never needed by the API
# container.  CI installs this file so local and hosted checks run the same toolchain.
-r requirements.txt

httpx2>=2.0,<3
matplotlib>=3.8,<4
pytest>=8.0,<10
ruff==0.15.21
